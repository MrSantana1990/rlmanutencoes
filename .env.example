# Copie para `.env.local` (dev) ou use variáveis no ambiente (staging/prod).

# -----------------------------------------------------------------------------
# Imagens (recomendado: URLs públicas da AWS S3/CloudFront)
# -----------------------------------------------------------------------------
# Se não definir, o site usa imagens locais em `client/public/images`.
VITE_HERO_IMAGE_URL=
VITE_TEAM_IMAGE_URL=
VITE_MECHANICAL_IMAGE_URL=
VITE_HYDRAULIC_IMAGE_URL=
VITE_PNEUMATIC_IMAGE_URL=
VITE_ELECTRICAL_IMAGE_URL=

# Versão opcional para forçar refresh de assets locais em produção (ex.: Netlify)
# Ex.: VITE_ASSET_VERSION=2026-02-06-01
VITE_ASSET_VERSION=

# -----------------------------------------------------------------------------
# Analytics (opcional)
# Se não definir, nenhum script é injetado.
# -----------------------------------------------------------------------------
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=
